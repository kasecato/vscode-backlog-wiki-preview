{
	"$schema": "http://json-schema.org/draft-04/schema#",
	"title": "Backlog contributions to package.json",
	"type": "object",
	"properties": {
		"contributes": {
			"type": "object",
			"properties": {
				"backlog.previewStyles": {
					"type": "array",
					"description": "Contributed CSS files that change the look or layout of the Backlog preview",
					"items": {
						"type": "string",
						"description": "Extension relative path to a css file"
					}
				},
				"backlog.previewScripts": {
					"type": "array",
					"description": "Contributed scripts that are executed in the Backlog preview",
					"items": {
						"type": "string",
						"description": "Extension relative path to a JavaScript file"
					}
				},
				"backlog.markdownItPlugins": {
					"type": "boolean",
					"description": "Does this extension contribute a markdown-it plugin?"
				}
			}
		}
	}
}